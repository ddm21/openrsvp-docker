version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: rsvp-postgres-local
    restart: unless-stopped
    command: postgres -c max_connections=20 -c shared_buffers=256MB
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: rsvp
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - rsvp_local_network
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G

  redis:
    image: redis:7-alpine
    container_name: rsvp-redis-local
    restart: unless-stopped
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    networks:
      - rsvp_local_network
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M

  adminer:
    image: adminer
    container_name: rsvp-adminer-local
    restart: unless-stopped
    ports:
      - "8080:8080"
    networks:
      - rsvp_local_network
    depends_on:
      - postgres
    deploy:
      resources:
        limits:
          memory: 128M

  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: rsvp-redis-commander-local
    restart: unless-stopped
    environment:
      - REDIS_HOSTS=local:redis:6379
    ports:
      - "8081:8081"
    networks:
      - rsvp_local_network
    depends_on:
      - redis
    deploy:
      resources:
        limits:
          memory: 128M

  rsvp-web:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    image: korexdotcf/openrsvp-web:latest
    container_name: rsvp-web-local
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"
    networks:
      - rsvp_local_network
    depends_on:
      - postgres
      - redis

networks:
  rsvp_local_network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
